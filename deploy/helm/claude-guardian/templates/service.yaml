apiVersion: v1
kind: Service
metadata:
  name: {{ include "claude-guardian.fullname" . }}
  labels:
    {{- include "claude-guardian.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "claude-guardian.selectorLabels" . | nindent 4 }}
